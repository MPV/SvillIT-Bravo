<p>
  <%= f.label :activity_id, 'Aktivitet' %><br />
  <%= f.collection_select :activity_id, Activity.find(:all, :order => :name), :id, :name, :include_blank => true %>
</p>
<p>
  <%= f.label :description, 'Resultatet blev' %><br />
  <%= f.text_area :description %>
</p>
