<h1>Nytt medlemsskap</h1>

<% form_for(@group_member) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <p>
    <%= f.submit 'Skapa' %>
  </p>
<% end %>

<%= link_to 'Tillbaka', group_members_path, { :class => 'button back' } %>