<h1>&Auml;ndrar facilitet</h1>

<% form_for(@facility) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= f.submit 'Uppdatera' %>
  </p>
<% end %>

<%= link_to 'Tillbaka', facilities_path, { :class => 'button back' } %>
<%= link_to 'Visa', @facility, { :class => 'button show' } %>