<h1>Alla aktiviteters deltagare:</h1>

<table class="index">
  <tr>
    <th>Medlem:</th>
    <th>Aktivitet:</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @participants.each do |participant| %>
  <tr>
    <td><%=h participant.user.login %></td>
    <td><%=h participant.activity.name %></td>
    <td><%= link_to 'Show', participant %></td>
    <td><%= link_to 'Edit', edit_participant_path(participant) %></td>
    <td><%= link_to 'Destroy', participant, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Ny deltagare', new_participant_path %>